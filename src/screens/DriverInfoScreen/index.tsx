import { View, Text } from 'react-native';

export const DriversInfoScreen = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>DriversInfoScreen</Text>
    </View>
  );
};
